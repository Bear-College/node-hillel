extends layout

block content
  h1= title

  if error
    p(style="color: red") #{error}
  else if users.length
    ul
      each user in users
        li= user.name
  else
    p Користувачів поки немає.
